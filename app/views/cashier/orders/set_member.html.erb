<div class="container">
  <%= render :partial => "shared/tabs1", :locals => { :selected_item => '訂單資訊' } %>

  <%= form_for [:cashier, @order] do |f|%>

  <%= f.label :member_id, "會員名稱" %>
  <%= f.collection_select  :member_id, Member.all, :id, :name, {prompt: true}, {class: "discount-list form-control"} %>
  <br>
  <%= f.submit "確認修改",class: "btn btn-primary " %>
  <% end %>

</div>